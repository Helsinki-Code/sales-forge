import{requireApiAccess}from"@/lib/api-access";import{apiError,ok}from"@/lib/api-response";
export async function GET(){try{const{workspace,supabase,principal}=await requireApiAccess("findings:read");let query=supabase.from("evidence_snapshots").select("id,site_id,run_id,kind,source_url,source_path,captured_at,summary,content_hash,sites!inner(name,workspace_id)").eq("sites.workspace_id",workspace.workspace_id);if(principal?.siteIds?.length)query=query.in("site_id",principal.siteIds);const{data,error}=await query.order("captured_at",{ascending:false}).limit(100);if(error)throw error;return ok(data)}catch(error){return apiError(error)}}
