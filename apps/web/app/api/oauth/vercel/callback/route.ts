import { encryptSecret } from "@seoforge/core";import { exchangeVercelCode } from "@seoforge/integrations";import { apiUser } from "@/lib/auth";import { requireWorkspace } from "@/lib/workspace";
export async function GET(request:Request){const auth=await apiUser();if(!auth)return Response.redirect(new URL("/login",request.url));const workspace=await requireWorkspace(auth.supabase,auth.user,"admin");const url=new URL(request.url);const code=url.searchParams.get("code");if(!code)return Response.redirect(new URL("/dashboard/providers?error=vercel",url));const tokens=await exchangeVercelCode(code,`${url.origin}/api/oauth/vercel/callback`);const label="Vercel";await auth.supabase.from("provider_connections").upsert({workspace_id:workspace.workspace_id,provider:"vercel",label,encrypted_credentials:encryptSecret(tokens,`${workspace.workspace_id}:vercel:${label}`),status:"active",last_verified_at:new Date().toISOString()},{onConflict:"workspace_id,provider,label"});return Response.redirect(new URL("/dashboard/providers?connected=vercel",url));}
