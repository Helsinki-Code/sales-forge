import { PageHeader } from "@/components/page-header";import { ResourceTable } from "@/components/resource-table";import { currentWorkspace } from "@/lib/auth";
export default async function Page(){const{supabase,membership}=await currentWorkspace();const{data=[]}=await supabase.from("audit_events").select("*").eq("workspace_id",membership?.workspace_id).order("created_at",{ascending:false}).limit(100);return <><PageHeader title="Audit log" description="Human decisions, provider events, mutations, and failures remain attributable."/><ResourceTable rows={data||[]} columns={[{key:"created_at",label:"Time",render:r=>new Date(r.created_at).toLocaleString()},{key:"action",label:"Action"},{key:"target_type",label:"Target"},{key:"target_id",label:"ID"}]}/></>}
