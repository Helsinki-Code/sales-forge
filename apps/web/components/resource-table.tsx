export function ResourceTable({columns,rows,empty="Nothing here yet."}:{columns:{key:string;label:string;render?:(row:any)=>React.ReactNode}[];rows:any[];empty?:string}){return <div className="table-card">{rows.length?<table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={row.id||i}>{columns.map(c=><td key={c.key}>{c.render?c.render(row):String(row[c.key]??"—")}</td>)}</tr>)}</tbody></table>:<div className="empty">{empty}</div>}</div>}
