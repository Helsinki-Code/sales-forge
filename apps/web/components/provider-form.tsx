"use client";
import { useState } from "react";
export function ProviderForm(){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const provider=String(form.get("provider"));const body={provider,label:form.get("label"),credentials:provider==="dataforseo"?{login:String(form.get("login")),password:String(form.get("password"))}:{apiKey:String(form.get("apiKey"))}};const res=await fetch("/api/v1/providers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const json=await res.json();setMessage(res.ok?"Connection encrypted and saved.":json.error?.message||"Connection failed");if(res.ok)e.currentTarget.reset();}
return <form className="card form" onSubmit={submit}><h2 style={{margin:0}}>Connect a provider</h2><div className="field"><label>Provider</label><select name="provider" defaultValue="dataforseo"><option value="dataforseo">DataForSEO</option><option value="bing">Bing / IndexNow</option></select></div><div className="field"><label>Connection label</label><input name="label" defaultValue="Primary" required/></div><div className="field"><label>DataForSEO login</label><input name="login" autoComplete="off"/></div><div className="field"><label>Password / API key</label><input name="password" type="password" autoComplete="new-password"/><input name="apiKey" type="hidden"/></div>{message&&<div className="notice">{message}</div>}<button className="button primary">Encrypt and connect</button></form>}
