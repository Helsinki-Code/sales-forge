"use client";
export function BillingActions(){async function go(path:string,body?:unknown){const response=await fetch(path,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body||{})});const json=await response.json();if(json.data?.url)location.href=json.data.url;else alert(json.error?.message||"Billing action failed");}return <div style={{display:"flex",gap:10,flexWrap:"wrap"}}><button className="button primary" onClick={()=>go("/api/billing/checkout",{plan:"starter"})}>Choose Starter</button><button className="button secondary" onClick={()=>go("/api/billing/checkout",{plan:"pro"})}>Choose Pro</button><button className="button secondary" onClick={()=>go("/api/billing/portal")}>Manage subscription</button></div>}
