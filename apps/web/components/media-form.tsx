"use client";import{useState}from"react";
export function MediaForm({sites}:{sites:{id:string;name:string}[]}){const[message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/v1/media/generate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({siteId:f.get("siteId"),kind:f.get("kind"),prompt:f.get("prompt"),sourceArticleUrl:f.get("sourceArticleUrl")||undefined,altText:f.get("altText")||undefined})});const j=await r.json();setMessage(r.ok?`Media job ${j.data.jobId} queued.`:j.error?.message||"Generation failed");}return <form className="card form" onSubmit={submit}><h2 style={{margin:0}}>Generate brand-aligned media</h2><div className="field"><label>Site</label><select name="siteId" required>{sites.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></div><div className="field"><label>Format</label><select name="kind"><option value="image">Image</option><option value="audio">Narration</option><option value="video">Short video</option></select></div><div className="field"><label>Source article URL</label><input name="sourceArticleUrl" type="url"/></div><div className="field"><label>Creative brief or narration script</label><textarea name="prompt" rows={7} required minLength={10}/></div><div className="field"><label>Alt text (images)</label><input name="altText" maxLength={125}/></div>{message&&<div className="notice">{message}</div>}<button className="button primary">Generate with credits</button></form>}
