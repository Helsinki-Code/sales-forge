"use client";
type Point={label:string;value:number};
export function TrendChart({points,label,color="#236a50",invert=false}:{points:Point[];label:string;color?:string;invert?:boolean}){const values=points.map(p=>p.value);const min=Math.min(...values,0),max=Math.max(...values,1);const coords=points.map((p,i)=>{const x=points.length===1?50:(i/(points.length-1))*100;const normalized=(p.value-min)/(max-min||1);return`${x},${invert?normalized*88+6:94-normalized*88}`}).join(" ");return <div className="trend-chart" role="img" aria-label={`${label}: ${points.map(p=>`${p.label} ${p.value}`).join(", ")}`}><svg viewBox="0 0 100 100" preserveAspectRatio="none"><defs><linearGradient id={`fill-${label.replace(/\W/g,"")}`} x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor={color} stopOpacity=".28"/><stop offset="1" stopColor={color} stopOpacity="0"/></linearGradient></defs><polygon points={`0,100 ${coords} 100,100`} fill={`url(#fill-${label.replace(/\W/g,"")})`}/><polyline points={coords} fill="none" stroke={color} strokeWidth="2" vectorEffect="non-scaling-stroke"/></svg><div className="chart-axis">{points.map(p=><span key={p.label}>{p.label}</span>)}</div></div>}
