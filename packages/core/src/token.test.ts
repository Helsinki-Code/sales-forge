import{beforeEach,describe,expect,it}from"vitest";import{createApiToken,hashApiToken,safeTokenHashMatch}from"./intelligence.js";
describe("API token security",()=>{beforeEach(()=>{process.env.API_TOKEN_PEPPER="test-pepper-that-is-longer-than-thirty-two-characters"});it("creates environment-specific opaque tokens",()=>{const value=createApiToken("test");expect(value.token).toMatch(/^sf_test_/);expect(value.hash).toHaveLength(64);expect(value.token).not.toContain(value.hash)});it("validates with constant-time hash comparison",()=>{const token=createApiToken("live");expect(safeTokenHashMatch(token.token,token.hash)).toBe(true);expect(safeTokenHashMatch(`${token.token}x`,token.hash)).toBe(false);expect(hashApiToken(token.token)).toBe(token.hash)})});
